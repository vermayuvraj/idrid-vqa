# Copy to .env and fill in. .env is git-ignored. Load in WSL with:
#   set -a; source .env; set +a
GEMINI_API_KEY=
OPENAI_API_KEY=      # optional — only needed for the GPT-4o-mini eval baseline
WANDB_API_KEY=       # optional — for wandb logging during training
